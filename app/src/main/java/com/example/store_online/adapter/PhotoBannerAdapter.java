package com.example.store_online.adapter;import android.app.Activity;import android.view.LayoutInflater;import android.view.View;import android.view.ViewGroup;import androidx.annotation.NonNull;import androidx.recyclerview.widget.RecyclerView;import androidx.viewpager2.widget.ViewPager2;import com.example.store_online.R;import com.example.store_online.data_models.PhotoBanner;import java.util.ArrayList;public class PhotoBannerAdapter extends RecyclerView.Adapter<PhotoBannerAdapter.PhotoHolder> {    private Activity activity;    private int layoutID;    private ArrayList<PhotoBanner> listBanner;    public PhotoBannerAdapter(Activity activity, int layoutID, ArrayList<PhotoBanner> listBanner) {        this.activity = activity;        this.layoutID = layoutID;        this.listBanner = listBanner;    }    @NonNull    @Override    public PhotoHolder onCreateViewHolder(@NonNull ViewGroup parent, int viewType) {        LayoutInflater inflater = activity.getLayoutInflater();        View view = inflater.inflate(viewType,parent,false);        return new PhotoHolder(view);    }    @Override    public void onBindViewHolder(@NonNull PhotoHolder holder, int position) {        PhotoBanner photoBanner = listBanner.get(position);//        Glide.with(activity)//                .load(photoBanner.getUri())//                .error(activity.getResources().getDrawable(R.drawable.ic_launcher_background)//                .into();    }    @Override    public int getItemCount() {        return listBanner.size();    }    public int getLayoutID() {        return layoutID;    }    public class PhotoHolder extends RecyclerView.ViewHolder{        private ViewPager2 viewPager2;        public PhotoHolder(@NonNull View itemView) {            super(itemView);            viewPager2 = itemView.findViewById(R.id.list_img_banner);        }    }}